# GPS / discovery / auth / design-system tests. Later-phase suites
# (chat, calls, matching, etc.) remain in the tree but are not executed here.
paths:
  - test/core
  - test/shared
  - test/widget_test.dart
  - test/features/discovery
  - test/features/location
  - test/features/authentication
  - test/features/boost
  - test/security
