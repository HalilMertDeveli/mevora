/// Semantic viewport bands for responsive layout (not device-specific tables).
abstract final class ResponsiveBreakpoints {
  static const double compactHeight = 700;
  static const double veryCompactHeight = 560;
  static const double narrowWidth = 360;
}
